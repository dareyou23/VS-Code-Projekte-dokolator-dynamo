{
  "name": "dokolator-dynamo",
  "version": "1.0.0",
  "description": "DynamoDB backend for Doppelkopf game statistics",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "sam build && sam deploy",
    "deploy:guided": "sam build && sam deploy --guided",
    "test:create": "npm run build && sam local invoke CreateGameFunction -e test-events/create-game.json",
    "test:list": "npm run build && sam local invoke ListGamesFunction -e test-events/list-games.json",
    "test:stats": "npm run build && sam local invoke GetStatsFunction -e test-events/get-stats.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-lambda": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.14.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.5.0"
  }
}
